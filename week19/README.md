
Bootcamp day 16 Team Project: Friends screen + API connection
- Created FriendCard component (avatar, name, location, about, likes)
- Dynamic rendering from API with useEffect + axios
- Display users with flex-wrap layout
- Tailwind styling for responsive layout (WIP)

Team Project: Friends list + profile tile
- FriendsContainer with shuffle/limit/grid
- UserDetailTile with isSelf logic + layout
- useFetch hook integration (reusable request logic)
- Random refresh with UUID key
- Tested multiple fetch setups + improved layout

Team Project: Friends list + profile tile
- FriendsContainer with shuffle/limit/grid
- UserDetailTile with isSelf logic + layout
- useFetch hook integration (reusable request logic)
- Random refresh with UUID key
- Tested multiple fetch setups + improved layout

 Bootcamp day 17 Team Project: Friends list + profile tile
- FriendsContainer with shuffle/limit/grid
- UserDetailTile with isSelf logic + layout
- useFetch hook integration (reusable request logic)
- Random refresh with UUID key
- Tested multiple fetch setups + improved layout
